[2.12]Which, if any, of the following names are invalid?
(a) int double = 3.14;    //invalid because "double" is reserved buy the language itself
(b) int _;                //invalid if defined outside a function (cannot begin with _), else it's valid but uninitialized
(c) int catch-22;         //invalid because only numbers, letters and _ can compose an identifier
(d) int 1_or_2 = 1;       //invalid because identifiers must begin with either a letter or an _
(e) double Double = 3.14; //valid but variables normally shoud be lowercase
